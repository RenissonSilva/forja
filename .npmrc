# expo-router@57 pulls optional web "DOM components" tooling (radix-ui/vaul) whose
# react-dom peer range is a patch ahead of the react version Expo SDK 57 pins.
# That mismatch is unrelated to this native-only app; legacy-peer-deps avoids the
# resulting ERESOLVE noise on every install.
legacy-peer-deps=true
